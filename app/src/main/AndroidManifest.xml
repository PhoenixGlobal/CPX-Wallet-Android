<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="chinapex.com.wallet">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".global.ApexWalletApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 新用户activity -->
        <activity
            android:name=".view.NewVisitorActivity"
            android:theme="@style/AppTheme.MyTranslucentNavigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 主activity -->
        <activity android:name=".view.MainActivity" />
        <activity android:name=".view.CreateWalletActivity" />
        <activity android:name=".view.WalletDetailActivity" />
        <activity android:name=".view.TransferActivity"></activity>
    </application>

</manifest>